#!/usr/bin/env node
// Placeholder: workstree is a Go binary distributed via GitHub releases.
// This npm package reserves the name and will later ship a binary-fetching wrapper.
console.error(
  "workstree is not distributed via npm yet.\n" +
    "Install: https://github.com/whoislikemiha/workstree (see Releases)\n"
);
process.exit(1);
