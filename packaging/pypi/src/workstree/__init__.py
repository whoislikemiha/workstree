"""workstree — worktrees that work on the first try.

Placeholder package reserving the name. The tool is a Go binary:
https://github.com/whoislikemiha/workstree (see Releases).
"""

import sys

__version__ = "0.0.1"


def main() -> int:
    sys.stderr.write(
        "workstree is not distributed via PyPI yet.\n"
        "Install: https://github.com/whoislikemiha/workstree (see Releases)\n"
    )
    return 1
